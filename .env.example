# HomeAI 前端环境变量示例
# 复制为 .env.local 后按需修改（.env.local 不会被提交）

# 后端 API 根地址（接入 Spring Boot 时填写）
# 例如：https://homeai.example.com/api 或 http://localhost:8080/api
VITE_API_BASE_URL=/api

# 数据来源模式：
#   true  -> 使用内置 mock 数据（默认，无需后端即可运行）
#   false -> 调用 VITE_API_BASE_URL 指向的真实后端
VITE_USE_MOCK=true
